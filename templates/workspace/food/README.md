# Reusable food facts

Store user-supplied product facts in `products/` and recipes in `recipes/`.
Record serving basis, source/date, known nutrients, unknowns, and recipe yield.
A matched exact label takes precedence over a generic estimate.

Actual portions consumed belong in `logs/nutrition/`. Never infer consumption
from this database.
