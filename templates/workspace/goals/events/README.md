# Optional event goals

Create a file only for an event the user chooses. Record date, format, goal,
official source, and verification date. Recheck event-specific rules before
using them. No sport, category, race load, or event is preselected.
